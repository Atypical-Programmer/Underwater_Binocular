# Metashape integration

Metashape is an external GUI consumer. Use `underwater_binocular.reconstruction.metashape` to write local-frame pose references, then follow the generated import instructions. Do not mark the result georeferenced without actual control points, and do not treat arbitrary reconstruction scale as independently verified underwater metric depth.

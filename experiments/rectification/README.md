# Rectification experiments

Use `geometry.rectification` for deterministic alpha, size, `P`, `Q`, ROI, and left-right consistency diagnostics. Alpha changes the virtual projection and valid field of view; it does not change the raw correspondence set. The recorded alpha 0/1 invariance result is retained under `validation/reference/20260802_150233/`.
